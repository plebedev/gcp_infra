apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-api.fullname" . }}
  labels:
    {{- include "rust-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "rust-api.selectorLabels" . | nindent 4 }}
